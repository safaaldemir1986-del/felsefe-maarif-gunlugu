[package]
name = "felsefe-maarif-gunlugu"
version = "0.1.0"
description = "Felsefe Maarif Günlüğü"
authors = ["Felsefe Maarif Günlüğü"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
